import styled from 'styled-components';

const FirstForm = () => {
  return (
    <FirstFormStyle>
      <div>First Form</div>
    </FirstFormStyle>
  );
};

const FirstFormStyle = styled.div`
  margin-top: 50px;
  text-align: center;
  color: green;
`;

export default FirstForm;
